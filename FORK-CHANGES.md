# Fork 修改说明

本分支基于 [zisenuomituan/qq-miao-miao-helper](https://github.com/zisenuomituan/qq-miao-miao-helper)（Apache-2.0，原作者 蓝鲸公益 LJGY）修改。

依 Apache License 2.0 第 4(b) 条，以下文件相对上游已被修改：

| 文件 | 改动 |
|---|---|
| `MeowEngine.java` | **新增** —— 文本变换核心，无 Android 依赖 |
| `MeowRewriter.java` | **新增** —— 增量重写状态机 |
| `TextProcessor.java` | 重写为配置适配层，变换逻辑移入 `MeowEngine` |
| `QQAccessibilityService.java` | 改用 `MeowRewriter`；修正光标处理 |
| `CatConfig.java` | 新增自定义替换规则、语气词位置两项配置 |
| `MainActivity.java` | 新增规则编辑卡片；预览改为读取界面当前状态 |
| `tools/verify/` | **新增** —— 引擎逻辑的验证脚本 |

---

## 修掉的 bug

### 1. 链接、邮箱、小数被拆坏（最严重）

上游的加喵正则是 `([。！？!?；;!?.~～]+)`，其中含 `.` 和 `?`，且没有任何保护区，
于是每条带链接的消息都会被破坏：

```
https://docs.qq.com/doc/abc  →  https://docs.喵qq.喵com/doc/abc
abc.def@qq.com               →  abc.喵def@qq.喵com
单价 3.14 元                  →  单价 3.喵14 元
@你好呀                       →  @主人好呀      （@ 不到人）
[你好]                        →  [主人好]        （表情失效）
```

现在 URL / 邮箱 / @提及 / `[表情]` / 反引号代码会先被替换成占位符，
变换完再还原；句末终止符也不再包含 `.`。

### 2. 多行消息被压成一行

上游 `stripEmoticons()` 结尾的 `replaceAll("\\s+", " ")` 把换行也当空白吃掉了。
现在换行作为句子边界保留。

### 3. 颜文字每次重抽

上游用 `random.nextInt()`，实时模式下每次重写都换一个，打字过程中一直闪。
现在由文本内容哈希决定，同一段文字永远选中同一个。

### 4. 光标位置

上游把 `SELECTION_START/END` 塞进了 `ACTION_SET_TEXT` 的参数里 —— 那两个参数
属于 `ACTION_SET_SELECTION`，在这里会被忽略。现在写完文本后单独发一次
`ACTION_SET_SELECTION` 把光标放到末尾。

### 5. 预览用的是已保存的配置

上游「测试当前配置」读的是 `SharedPreferences`，所以必须先保存才能看到效果，
「先测试、满意再保存」走不通。现在预览读取界面上的当前状态。

---

## 新增能力

### 自定义替换规则

上游只有 `你→主人`、`我→本喵` 两个写死的开关。现在可以在界面里自己写，
每行一条 `原文=替换`：

```
我们=我们这群喵
老板=铲屎官
# 以 # 开头的行会被忽略
```

两处关键保证：

- **最长优先**：同时存在 `我` 和 `我们` 时，`我们` 优先匹配。
- **只扫描一趟**：`我=本喵` 与 `喵=呜` 并存时，不会把刚生成的「本**喵**」
  再改成「本**呜**」。上游那种链式 `String.replace()` 一定会踩这个坑。

### 语气词位置可选

上游固定是「今天真好**！喵**」。现在默认「今天真好**喵！**」，
界面上可以切回原版行为。

### 自指规则不再套娃

`我们=我们这群喵` 这类规则的替换结果含有自身原文。增量重写模式下整个输入框
会被反复变换，上游的做法会让它无限膨胀成
「我们这群喵这群喵这群喵…」。

`MeowRewriter` 的解法是**记住用户的原文**：框里的内容若以上次写入的结果开头，
说明用户只是在后面继续敲，把新敲的那截接回原文即可 —— 变换永远作用在纯用户
输入上。用户在中间插字导致原文无法还原时，退化路径会临时摘掉自指规则以保证收敛。

### 拼音组词保护

有些输入法用 `setComposingText` 把拼音直接写进 EditText。在那个状态下改写文本
会打乱输入法。现在会检测「结尾是连续 ASCII 字母且前文含中文」并跳过。

---

## 一个必须说清楚的限制

**这个 App 做的是「编辑时改写」，不是「发送时改写」。**

无障碍服务拦不住发送动作：

- `TYPE_VIEW_CLICKED` 是点击**派发之后**才送达的，收到时消息已经发出去了；
- `onKeyEvent()` 只接收硬件按键，软键盘的发送键不走它。

所以这里保证的是「输入框里的内容随时都已经是喵化的」，发送时机因此变得无关紧要。
如果你要的是「编辑时看不出异常、按下发送才变」，无障碍这条路走不通，需要换成
QQNT 插件（PC）、LSPosed 模块（需 root）或自定义输入法接管回车键。

---

## 验证

没有 Android 设备也能验证引擎逻辑：

```bash
node --test tools/verify/verify.test.mjs
```

`tools/verify/transcribe.mjs` 是 `MeowEngine.java` + `MeowRewriter.java` 的逐行
JS 转写，结构与 Java 一一对应。30 个用例覆盖上述全部 bug 的回归、引擎核心行为
和状态机。

**注意**：它验证的是**逻辑**，不是 Java 代码本身。改动 Java 之后需要同步改转写，
两边才有意义。真正的编译与真机行为仍需 Android Studio 验证。
