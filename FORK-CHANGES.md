# Fork 修改说明

本分支基于 [zisenuomituan/qq-miao-miao-helper](https://github.com/zisenuomituan/qq-miao-miao-helper)（Apache-2.0，原作者 蓝鲸公益 LJGY）修改。

依 Apache License 2.0 第 4(b) 条，以下文件相对上游已被修改：

| 文件 | 改动 |
|---|---|
| `MeowEngine.java` | **新增** —— 文本变换核心，无 Android 依赖 |
| `MeowRewriter.java` | **新增** —— 增量重写状态机 |
| `TextProcessor.java` | 重写为配置适配层，变换逻辑移入 `MeowEngine` |
| `QQAccessibilityService.java` | 改用 `MeowRewriter`；修正光标处理 |
| `CatConfig.java` | 新增自定义替换规则、语气词位置两项配置 |
| `MainActivity.java` | 新增规则编辑卡片；预览改为读取界面当前状态 |
| `tools/verify/` | **新增** —— 引擎逻辑的验证脚本 |

---

## 修掉的 bug

### 1. 链接、邮箱、小数被拆坏（最严重）

上游的加喵正则是 `([。！？!?；;!?.~～]+)`，其中含 `.` 和 `?`，且没有任何保护区，
于是每条带链接的消息都会被破坏：

```
https://docs.qq.com/doc/abc  →  https://docs.喵qq.喵com/doc/abc
abc.def@qq.com               →  abc.喵def@qq.喵com
单价 3.14 元                  →  单价 3.喵14 元
@你好呀                       →  @主人好呀      （@ 不到人）
[你好]                        →  [主人好]        （表情失效）
```

现在 URL / 邮箱 / @提及 / `[表情]` / 反引号代码会先被替换成占位符，
变换完再还原；句末终止符也不再包含 `.`。

### 2. 多行消息被压成一行

上游 `stripEmoticons()` 结尾的 `replaceAll("\\s+", " ")` 把换行也当空白吃掉了。
现在换行作为句子边界保留。

### 3. 颜文字每次重抽

上游用 `random.nextInt()`，实时模式下每次重写都换一个，打字过程中一直闪。
现在由文本内容哈希决定，同一段文字永远选中同一个。

### 4. 光标位置

上游把 `SELECTION_START/END` 塞进了 `ACTION_SET_TEXT` 的参数里 —— 那两个参数
属于 `ACTION_SET_SELECTION`，在这里会被忽略。现在写完文本后单独发一次
`ACTION_SET_SELECTION` 把光标放到末尾。

### 5. 预览用的是已保存的配置

上游「测试当前配置」读的是 `SharedPreferences`，所以必须先保存才能看到效果，
「先测试、满意再保存」走不通。现在预览读取界面上的当前状态。

---

## 新增能力

### 自定义替换规则

上游只有 `你→主人`、`我→本喵` 两个写死的开关。现在可以在界面里自己写，
每行一条 `原文=替换`：

```
我们=我们这群喵
老板=铲屎官
# 以 # 开头的行会被忽略
```

两处关键保证：

- **最长优先**：同时存在 `我` 和 `我们` 时，`我们` 优先匹配。
- **只扫描一趟**：`我=本喵` 与 `喵=呜` 并存时，不会把刚生成的「本**喵**」
  再改成「本**呜**」。上游那种链式 `String.replace()` 一定会踩这个坑。

### 语气词位置可选

上游固定是「今天真好**！喵**」。现在默认「今天真好**喵！**」，
界面上可以切回原版行为。

### 自指规则不再套娃

`我们=我们这群喵` 这类规则的替换结果含有自身原文。增量重写模式下整个输入框
会被反复变换，上游的做法会让它无限膨胀成
「我们这群喵这群喵这群喵…」。

`MeowRewriter` 的解法是**记住用户的原文**：框里的内容若以上次写入的结果开头，
说明用户只是在后面继续敲，把新敲的那截接回原文即可 —— 变换永远作用在纯用户
输入上。用户在中间插字导致原文无法还原时，退化路径会临时摘掉自指规则以保证收敛。

### 拼音组词保护

有些输入法用 `setComposingText` 把拼音直接写进 EditText。在那个状态下改写文本
会打乱输入法。现在会检测「结尾是连续 ASCII 字母且前文含中文」并跳过。

---

## 停不下来的问题（v2 修复）

上游版本**没有总开关** —— 只有「你→主人」「断句加喵」这几个分项开关，
想让它安静下来只能一个个关掉，而且关完之后 `stripEmoticons()` 里那句
`replaceAll("\s+", " ")` 照样会把多行消息压成一行。实际效果就是「关不掉」。

本分支补上：

- **总开关**：主界面顶部，关掉后服务收到事件立刻返回，连输入框都不去读。
  立即生效，不需要点「保存设置」。排队中的去抖任务也会被撤销。
- **快捷设置磁贴**：下拉通知栏一键暂停/恢复，不用打开 App（Android 7.0+）。
- **状态区分**：未授权 / 已暂停 / 运行中三种状态分别显示，不再只有开关两态。
- **「如何彻底停用」说明卡片**，写明暂停、停止、移除三级操作。

### 为什么卸载 QQ 没用

改写输入框的是**本应用的无障碍服务**，它不属于 QQ。卸载 QQ 只是移除了被改写的
对象，服务本身还在系统里注册着、权限还授予着，装回 QQ 立刻又开始工作。

正确的停用路径是三级：

| 想要的效果 | 操作 |
|---|---|
| 临时停 | 总开关，或下拉通知栏的「喵化开关」 |
| 彻底停止 | 系统设置 → 无障碍 → 已安装的服务 → QQ喵喵助手 → 关闭 |
| 完全移除 | 卸载本应用 |

---

## v3：句号确认键 + 冻结前缀（交互重做）

### 旧模型的结构性问题

原来有「标点触发」和「实时处理」两种模式，共同点是**每次触发都把整个
输入框基于「用户原文」重算一遍**。实测暴露出三个问题：

| 问题 | 实测 |
|---|---|
| 删标点也触发，并污染原文追踪 | `我今天很开心。` 删句号再打字 → `本喵今天很开心喵啊喵。` 一句两个喵 |
| 已显示的文字被回溯修改 | 实时模式下 `开心喵` 打着打着变回 `开心` |
| 逐词输入疯狂抖动 | 两字一顿打一句话 → **重写 6 次**，喵跟着光标满地跑 |

第三个是根子：**只有「句尾加喵」会抖**，因为「句尾」这个位置随打字后移；
关键词替换位置固定，完全不抖（对照实验：关掉加喵后 6 次重写降为 1 次）。

### 新模型

**中文句号是确认键。** 打字过程中输入框完全不动，打出 `。` 才把这句
转成喵喵语气 —— 而且**句号本身被吃掉**，因为中文聊天没人用句号结尾。

| 符号 | 触发 | 结果 |
|---|---|---|
| `。` | ✓ | **吃掉** |
| `。。`（及以上） | ✓ | 折成 `...` |
| `？` `！` | ✓ | 保留 |
| `？！` `！？` | ✓ | 保留，走惊讶组 |
| 英文 `,` `.` `?` `!` `...` | 不触发 | — |
| 回车、中文逗号 | 不触发 | — |

封过的句子**冻结、永不重算**，所以上面三个问题在结构上就不可能发生。
用户删进冻结区则整体重置。

实测：逐字打一句话 **写回 1 次**（旧实现 6 次），没有句号时**一次都不写**。

### ？！ 用回滚重做，不用延迟

`？！` 是独立的「难以置信」情绪。要识别它，直觉做法是打 `？` 后等一秒
看有没有 `！`，但那样输入框会「自己动」——恰好毁掉新模型最值钱的性质。

改成：打 `？` 立刻出疑问版，若**紧接着**打 `！`，就撤销刚才那次封句、
按 `？！` 重做成惊讶版。零延迟，且输入框只在按键瞬间变化。

---

## v3：颜文字改为标签交集

### 为什么不是简单的「情绪 → 池子」

关键词想要「感激」、标点想要「兴奋」，两个独立池子的交集**必然是空的**。
所以改成**给每个颜文字打标签**，两边各自要求某个标签，再取交集：

```
兴奋,感激,开心 = ヾ(=^･ω･^=)ノ      同时属于三个标签
```

四级降级保证一定有结果：交集 → 只看关键词 → 只看标点 → 全库。
交集为空时**关键词赢**，因为它比标点具体。

### 关键词扫描：最长优先 + 最后命中

两条规则单独用都不够，组合方式还很讲究：

- 只用「最长优先」：`刚才难过，现在开心` 分不出该用哪个
- 只用「最后命中」：`别难过` 会输给从第 1 位开始的 `难过` —— **正好搞反**

正确做法是**左到右最长优先扫描**（命中就吃掉该段不回头），再取**最后一个**。
最长优先决定「同一位置谁赢」，最后命中决定「不同位置谁赢」。实测 5/5 通过，
朴素的「起始位置最靠后」错 2/5。

### 排除短语

`不喜欢 = `（右边留空）表示命中即**弃权**：既不用它的情绪，也不让更短的
`喜欢` 接管，直接降级到标点判断。适合否定了但谈不上相反情绪的说法。
`讨厌` 同理 —— 撒娇和真生气时都会用，歧义太大不参与判断。

### 配置格式：标签必须在左边

```
平静,日常 = (=^･ω･^=)      ✓
(=^･ω･^=) = 平静,日常       ✗ 会被切在脸中间
```

**颜文字自己就含 `=`（猫脸的眼睛就是 `=`）**，写在分隔符左边的话
`indexOf('=')` 找到的是脸里那个。这个坑在原型阶段就踩到了。

默认库放在 `res/raw/kaomoji_lib.txt` 与 `res/raw/kaomoji_keywords.txt`，
纯文本便于修改扩充；界面上可以覆盖，也可以一键恢复默认。

### 标点与颜文字的合并

颜文字末尾自带同类标点时吃掉句尾那个，避免 `？ (=･ｪ･=)?` 双问号：

```
在吗喵？ + (=･ｪ･=)?   →  在吗喵 (=･ｪ･=)?     颜文字代劳
在吗喵？ + (=ʘωʘ=)    →  在吗喵？ (=ʘωʘ=)    保留
真的喵？！ + (=･ｪ･=)? →  真的喵？！ (=･ｪ･=)?  组合标点一律保留
```

按**选中的那个颜文字**判断，不是按它属于哪个组 —— 同组里也有不带问号的，
按组判会让问号凭空消失。

### 连打的句号 → 省略号

句号被吃掉了，那连打两个呢？两个句号在中文聊天里就是省略号的意思，
所以第二个 `。` 走与 `？！` 同一套**回滚重做**：撤销刚才那次封句，
改用 `...` 重新封一遍，情绪走【无奈】组。

```
我好累。      →  本喵好累喵 (=＿ω＿=)
我好累。。     →  本喵好累喵... (=＿ω＿=)
我好累。。。    →  本喵好累喵... (=＿ω＿=)     第三个起被吸收
```

回滚时连颜文字的「避开上次」游标一并退回，否则同一句话只因为多敲了
一个标点就会换一张脸，看起来像 bug。

注意英文的 `...` 依然**不触发** —— 只有中文句号能当确认键。

### 情绪合成表

标签交集有个真实的空洞：「什么意思！」。关键词说它困惑、标点说它兴奋，
两个池子毫无交集，而且保哪边都不对 —— 困惑组的脸自带问号，兴奋组又太欢快。
它真正的语气是【难以置信】，也就是惊讶。

这种「两种情绪叠起来等于第三种」没法从标签本身推出来，只能列表。
写在颜文字库末尾，`@merge` 开头：

```
@merge 困惑 + 兴奋 = 惊讶
@merge 疑问 + 兴奋 = 惊讶
```

降级链因此变成五级：**交集 → 合成表 → 只看关键词 → 只看标点 → 全库**。
合成表漏了什么也不会让句子拿不到颜文字，只是退回原来的行为。

顺带把选颜文字要用的三份数据（库、合成表、关键词）收进了
`KaomojiLib.Pack` —— 它们总是同生同死，以后再加词表不用改一路上的签名。

### 导入词库，并把情绪与符号拆成两个轴

原来一张脸只挂一组标签，「疑问」既是情绪又是句尾，结果交集几乎必然为空。
实测下来 65 个（情绪 × 句尾）组合里只有 9 个真有交集 —— 也就是说 83% 的
句子，句尾语气根本没进入选择，标点池形同虚设。

根治办法是拆成两个独立的轴：

```
[平静 | 句号,问号,叹号,省略号,波浪]
(=・ω・=) | (=.w.=)
```

左边是情绪（关键词决定），右边是符号（句尾标点决定），竖线后面是可选的
纯 ASCII 降级版。每张脸都声明自己适合哪几种句尾，交集才有得可取。

顺带导入了一份外部词库（126 条颜文字 / 164 关键词 / 125 排除短语），
与原有的合并而不是替换。合并后：

| | 原来 | 现在 |
|---|---|---|
| 颜文字 | 41 | 153 |
| 情绪标签 | 18 | 23 |
| 关键词 | 45 | 180 |
| 排除短语 | 6 | 125 |
| 交集直接命中 | 14% | **75%** |
| 加上合成表 | 17% | **91%** |
| 完全拿不到 | 0 | 0 |

剩下 9% 仍然走降级（退回情绪组），都是「明天见？」「加油？」这类
现实中几乎不会出现的组合。

几个具体决定：

- **句式不再用 `=` 分割**。猫脸的眼睛就是 `=`，迟早会切错。改用方括号
  和竖线 —— 这两个字符在全部 153 张脸和 126 个降级版里一次都没出现过。
- **困惑/思考不挂「问叹」**。外部词库给它们挂了，但那样交集直接就命中，
  合成表永远轮不上，「真的假的？！」就拿不到难以置信的脸了。
- **`@merge * + 问叹 = 惊讶`**。`？！` 本身就是难以置信，这层语气盖过句子
  原本的情绪——「好难过？！」重点不在难过而在不敢相信。`*` 是通配符，
  具体规则写在前面就能赢过它。
- **冲突时保留原有的**。外部词库把「别难过 / 别哭 / 不要难过 / 别生气 /
  不开心」当排除短语（只屏蔽、不给情绪），我们这边给了明确的安慰/难过。
- **半角句号、英文省略号依然不触发**。外部词库的触发规则里有，没采用：
  `3.14`、`docs.qq.com` 会被拆坏。
- **波浪 `～` 和爱心 `♡` 只导了数据、没开启触发**。要开的话改
  `MeowCommitter.isTerm` 即可，库那边不用动。
- **ASCII 降级版目前只用于防重复叠加**（粘来的降级写法也能认出来）。
  要做真正的字体缺字自动降级，靠 `Paint.hasGlyph()`（API 23，恰好是本项目
  的 minSdk），还没接。

词库是生成的，生成器在 `build-lib/`（不在仓库里）。`tools/verify/lint.mjs`
检查引用完整性，`tools/verify/coverage.mjs` 统计降级链各级的分布。

### 没有情绪关键词时走默认组（@default）

拆成情绪/符号两个轴之后引入的回归，实测才发现：

```
加了个符号和关键词判断的语气检查喵！ (=T_T=)
```

这句话一个情绪关键词都没命中，于是降到「只看符号」。问题在于拆轴之前
`punctTag('')` 返回的是【情绪】「平静」，这一级恰好等于「平静组」；拆成符号
轴之后，它的含义悄悄变成了「全部 124 张配得上叹号的脸」—— 哭的、生气的、
害怕的全在里面等概率抽。一句技术描述配上张哭脸，看起来就像标签打错了。

新增一级降级：没有情绪信号本身就是一种信号，该走平静组。用颜文字库里的
`@default 平静` 声明，没写则退回老行为。完整的降级链：

```
交集 → 合成表 → 只看情绪 → 默认组∩符号 → 默认组 → 只看符号 → 全库
```

同时把合成表的查询提到了「没命中关键词」也会跑 —— 通配规则
`@merge * + 问叹 = 惊讶` 的「不管原本什么情绪」本来就包含「没有情绪」，
「改完了吗？！」该是惊讶而不是平静（平静组里一张配得上问叹的脸都没有）。

新增 `tools/verify/trace.mjs`，把一句话的选择过程逐级打印出来 ——
这个 bug 就是用它定位的。

---

## v3：其它

- **支持微信**，默认打开。引擎读的是「聚焦且可编辑」的节点，不认任何
  软件专属的 view id，所以只是 `ChatApps` 里添一行。配套修了两个坐标：
  - 升级上来的老用户 `enabled_apps` 里只有 QQ，新加的会是关的 —— 记一份
    「见过哪些」的集合，没见过的默认打开（`CatConfig.adoptNewApps`）。
  - 原来把空集合当成「没设置过」，于是把勾全取消反而会变回全开。
    只有 QQ 一项时看不出来，两项之后就是真 bug 了。
- **切换聊天软件时清掉封句状态**。不清也多半会因为前缀对不上而自动重置，
  但那是碰巧，换应用这件事是确定的。
- **去掉 `packageNames`**：vivo 会拦截「声明监听 QQ 包名」的无障碍服务，
  A/B 验证过（去掉就能开）。代码里 `enabledPackages` 那层过滤仍在，
  功能不受影响，只是多收到些别的应用的事件然后立即丢弃。
- **删掉「处理模式」下拉框**：只剩一种交互，这个选项没有存在意义。
  顺带解决了「改了不点保存不生效」——那个 Spinner 根本没有监听器。
- **去掉「保存设置」按钮**：所有配置改动即时生效，与顶部总开关行为一致。
- **预览走真实封句路径**，包括颜文字挑选，所见即所得。
- **`java.util.function.Consumer` 换成自定义接口**：它是 API 24+，
  本项目 minSdk 23 且未开 desugaring。

---

## 一个必须说清楚的限制

**这个 App 做的是「编辑时改写」，不是「发送时改写」。**

无障碍服务拦不住发送动作：

- `TYPE_VIEW_CLICKED` 是点击**派发之后**才送达的，收到时消息已经发出去了；
- `onKeyEvent()` 只接收硬件按键，软键盘的发送键不走它。

所以这里保证的是「输入框里的内容随时都已经是喵化的」，发送时机因此变得无关紧要。
如果你要的是「编辑时看不出异常、按下发送才变」，无障碍这条路走不通，需要换成
QQNT 插件（PC）、LSPosed 模块（需 root）或自定义输入法接管回车键。

---

## 验证

没有 Android 设备也能验证引擎逻辑：

```bash
node --test tools/verify/verify.test.mjs
```

`tools/verify/transcribe.mjs` 是 `MeowEngine.java` + `MeowRewriter.java` 的逐行
JS 转写，结构与 Java 一一对应。51 个用例覆盖上述全部 bug 的回归、引擎核心行为
和状态机。

**注意**：它验证的是**逻辑**，不是 Java 代码本身。改动 Java 之后需要同步改转写，
两边才有意义。真正的编译与真机行为仍需 Android Studio 验证。
